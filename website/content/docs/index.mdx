---
title: Getting Started
description: Prime-Agent Web UI for local session traces, recursive RLM agents, persistent IPython state, tools, thinking, and coding-agent history.
---

import { Cards, Card } from 'fumadocs-ui/components/card';
import { Callout } from 'fumadocs-ui/components/callout';

## What is Prime-Agent Session Manager?

Prime-Agent Session Manager is a cross-platform Prime-Agent Web UI for browsing, searching, and managing Prime-Agent, [Pi](https://github.com/badlogic/pi-mono), and other coding-agent sessions. It is derived from [Pi Session Manager](https://github.com/Dwsy/pi-session-manager) by Dwsy and works as a native desktop app, responsive web app, and headless server.

> "The best tool is the one that gets out of your way." — Jef Raskin

Whether you're reviewing past conversations, searching for a specific code snippet across hundreds of sessions, or tracking your AI-assisted development workflow on a Kanban board, Prime-Agent Session Manager keeps the work connected.

### Key Capabilities

- **Session Browser** — List, project, directory, and Kanban views with favorites, batch export, and multi-path scanning. See [Session Browser](./session-browser).
- **Search and Recall** — SQLite FTS5, role filters, session ID matching, snippet highlighting, and command palette search. See [Search](./search).
- **Dashboard and Analytics** — Activity heatmap, project mix, model usage, token costs, and achievement tracking. See [Dashboard](./dashboard).
- **API and Automation** — HTTP, WebSocket, and SSE APIs share one command router for programmatic access. See [API Reference](./api).
- **Extension System** — Built-in, npm, and local path plugins with a browser-plugin SDK for app views, commands, tools, and settings. See [plugin SDK](../../docs/PSM_PLUGIN_SDK.md) and [Extensions README](../../extensions/README.md).
- **Pi Live and Bridge Sync** — Live session sync, context recall, tags, and bridge-based workflows for Pi CLI users. See [psm-bridge](./plugins/psm-bridge).
- **Terminal and Workspace Tools** — Integrated PTY terminal, session resume, hierarchical tags, and multi-platform support. See [Terminal](./terminal) and [Tags](./tags).

## UI Preview

| Home | Session Page |
| --- | --- |
| ![Home](/screenshots/home.png) | ![Session Page](/screenshots/session-page.png) |
| Session Tree | Kanban |
| ![Session Tree](/screenshots/session-tree.png) | ![Kanban](/screenshots/kanban.png) |

## Quick Install

### Desktop App

Download the latest fork release for your platform from [GitHub Releases](https://github.com/dat-lequoc/prime-agent-session-manager/releases):

| Platform | File |
|----------|------|
| macOS (Apple Silicon) | `Prime.Agent.Session.Manager_*_aarch64.dmg` |
| macOS (Intel) | `Prime.Agent.Session.Manager_*_x64.dmg` |
| Windows (x64) | `Prime.Agent.Session.Manager_*_x64-setup.exe` / `.msi` |
| Linux | `.deb` / `.AppImage` / `.rpm` |

### CLI / Headless Server

Download the `pi-session-cli` binary from the same release page, or build with Docker:

```bash
docker build -f Dockerfile.cli -t pi-session-cli .
docker cp $(docker create pi-session-cli):/pi-session-cli ./pi-session-cli
chmod +x pi-session-cli
./pi-session-cli
```

<Callout type="tip">
Interactive static demo: [Open the browser demo](https://dat-lequoc.github.io/prime-agent-session-manager/demo/)

Upstream static dataset browser: [https://dwsy.github.io/pi-session-manager/dataset/](https://dwsy.github.io/pi-session-manager/dataset/)
</Callout>

<Callout type="info">
Pi must be installed for session resume and terminal integration. Install it from [github.com/badlogic/pi-mono](https://github.com/badlogic/pi-mono).
</Callout>

## First Launch

1. **Open the app** — sessions from `~/.pi/agent/sessions/` are scanned automatically
2. **Browse sessions** — switch between list, project, directory, or Kanban views
3. **Search** — press `Cmd/Ctrl + K` for the command palette, or `Cmd/Ctrl + F` for sidebar search
4. **View a session** — click any session to open the viewer with tree navigation and flow visualization
5. **Open terminal** — press `Cmd/Ctrl + J` to toggle the built-in terminal

## What's Next?

<Cards>
  <Card title="Installation" href="./installation" description="Detailed setup for all platforms" />
  <Card title="Session Browser" href="./session-browser" description="Browse and organize your sessions" />
  <Card title="Search" href="./search" description="Full-text search with filters" />
  <Card title="Dashboard" href="./dashboard" description="Activity metrics and usage trends" />
  <Card title="API Reference" href="./api" description="HTTP, WebSocket, and SSE commands" />
  <Card title="psm-bridge" href="./plugins/psm-bridge" description="Live sync and bridge workflows" />
</Cards>
