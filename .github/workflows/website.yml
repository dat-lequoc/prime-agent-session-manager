name: Deploy Website

on:
  push:
    branches: [main]
    paths:
      - 'website/**'
      - 'src/**'
      - 'public/**'
      - 'index.html'
      - 'demo.html'
      - 'dataset.html'
      - 'vite.config.ts'
      - 'package.json'
      - 'package-lock.json'
      - 'pnpm-lock.yaml'
      - '.github/workflows/website.yml'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-node@v6
        with:
          node-version: 22

      - uses: pnpm/action-setup@v6

      - name: Install dependencies
        working-directory: website
        run: npm ci

      - name: Build
        working-directory: website
        run: npm run build

      - name: Install app dependencies
        run: pnpm install --frozen-lockfile

      - name: Build static demo page
        run: pnpm run build:demo --base /prime-agent-session-manager/demo/

      - name: Build static dataset page
        run: pnpm run build:dataset --base /prime-agent-session-manager/dataset/

      - name: Merge demo and dataset into website artifact
        run: |
          mkdir -p website/out/demo
          cp -R dist-demo/* website/out/demo/
          mkdir -p website/out/dataset
          cp -R dist-dataset/* website/out/dataset/

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v5
        with:
          path: website/out

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
